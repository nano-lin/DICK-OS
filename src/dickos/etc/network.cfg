# DICK/OS network configuration foundation
# DickNet does not exist yet, so this file intentionally defines no network
# behaviour. Future DickNet settings will use the shared config v1 format.

format_version = 1
