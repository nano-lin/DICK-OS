# DICK/OS service configuration foundation
# dickd and service autostart do not exist yet, so no services are declared.
# Future service settings will use the shared config v1 format.

format_version = 1
